-- Ya no usamos esta función porque la consulta se pasó directamente a PHP (querys.php).
-- Ejecuta la siguiente línea en tu gestor de base de datos (pgAdmin/DBeaver)
-- para limpiarla de tu esquema.

DROP FUNCTION IF EXISTS fun_buscar_productos(VARCHAR);
